# Changelog - App Pages & Global Styles

## 2026-03-12T00:06:00-04:00
- **Refactor (Testimonials Marquee)**: Optimizado el `animate-marquee-slow` del carrusel de testimonios en `page.tsx`. Se agregó un ancho real (`w-max`), y pausas on hover. El tiempo en `globals.css` se ajustó para asegurar un loop continuo y preciso en todas las resoluciones.
